package com.example.droidpromptplugin.codecompletion


/*

import com.intellij.codeInsight.inline.completion.*
import com.intellij.codeInsight.inline.completion.InlineCompletionContributor
import com.intellij.codeInsight.inline.completion.session.InlineCompletionContext


class InlineCompletionContributorImpl : InlineCompletionContributor {
    override fun getSuggestion(
        request: InlineCompletionRequest,
        context: InlineCompletionContext,
        result: InlineCompletionResultHandler
    ) {
        val mockText = " // Suggested by DroidPrompt AI"
        val suggestion = InlineCompletionElement(mockText)

        result.addElement(suggestion)
    }
}*/
